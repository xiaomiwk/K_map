1. 放到到17、18倍时，官方的和开发的显示内容不一样，默认的倍数上限也不一样，一个19，一个20.

2. marker弹出的window无法通过代码关闭

3. centerAtMBR无效，所有倍数下无效？

4. Circle的"x1,y1,r"中的半径r的度量单位？

5. 高速专题模式无法使用

6. 绘制轨迹时，需要延时一下才能绘制，可能是等待地图加载完毕，缺乏事件？百度的tilesloaded

7. 地址匹配服务如何访问

8. JavaScript 开发接口示例

9. 异常：关键字不在字典中F跟踪， while (_标记缓存.ContainsKey(__号码.Id) && _跟踪开始时间[__号码.Id] == __时间)

10.地址匹配服务，业务地理关联服务
